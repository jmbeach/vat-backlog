---
id: vat-72k
---

Cover: the problem (agents and humans claiming work from a shared backlog without stepping on each other); the design (tasks as plain markdown, versioned in git, each with a short addressable ID, with overflow detail in items/<id>.md); the two first-class forms — the Rust binary and the zero-install /vat skill an agent follows directly; the dogfooding story (VAT was built with VAT — the skill operated the backlog before the binary existed; point at the vat-backlog repo history); why it's good for humans too (low-friction, compact, simple); and optionally the atomic claim loop for shared/remote backlogs and the multi-agent ROYGBIV/roy workflow it enabled.
