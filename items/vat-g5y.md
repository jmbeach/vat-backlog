---
id: vat-g5y
---

Greedy front-loaded marker matcher: `[id]`, `[in-progress]`, `[by:<name>]`, `[blocked-by:<id>]`. Unknown `[...]` falls through to title. Empty title is an error. Property test: tokenize → serialize round-trip preserves canonical-form bullets. FMT-MARK-001..007, FMT-PARSE-006.
